# Day 1 notes — the rules, in my own words

Fill this in as you re-read the subject. If you can't paraphrase a rule
in one sentence, you don't understand it yet — that's what to ask about
before Day 3.

## Zone types and their movement cost
- normal:
- restricted:
- priority:
- blocked:

## Capacity rules
- What does `max_drones` mean, and what's special about start/end zones?
- What does `max_link_capacity` mean?

## The restricted-zone transit rule
- What exactly happens when a drone enters a restricted zone? (2 turns,
  can't wait mid-flight — explain it as if to a teammate.)

## Output format
- What does one line of output represent?
- What does `D<ID>-<zone>` mean vs `D<ID>-<connection>`?
- When is a drone omitted from a turn's line?

## Parser rules I must enforce
- (list every validation rule from Chapter VII.4 here)
